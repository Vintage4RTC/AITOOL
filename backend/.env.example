# Backend environment
PORT=8787
OPENAI_API_KEY=your_openai_key_here
# Slack: either a webhook or a bot token + channel
SLACK_WEBHOOK_URL=
# For email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
MAIL_FROM="QA Agent <qa-agent@example.com>"
MAIL_TO=you@example.com
# Public base URL (so Slack/email can link to artifacts). In dev, use http://localhost:8787
PUBLIC_BASE_URL=http://localhost:8787
